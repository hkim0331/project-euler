(ns hkim0331.p64)


;; under construction
;; mathemaica has ContinuedFraction[] function,
;; I know using the mathematica function a tidal way.

(defn floor
  [n]
  (int (Math/floor n)))


(floor (Math/sqrt 2))
